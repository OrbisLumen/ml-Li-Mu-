# 序列数据
- 实际中很多数据是有时序结构的

## 统计工具
- 在时间 $t$ 观察到 $x_t$，那么得到 $T$ 个不独立的随机变量 $(x_1, \cdots x_T) \sim p(x)$
- 使用条件概率展开
    $$ p(a,b) = p(a)p(b|a) = p(b)p(a|b) $$

## 自回归模型
- 对条件概率建模
    $$ p(x_t|x_1,\cdots x_{t-1}) = p(x_t|f(x_1,\cdots x_{t-1})) $$
  - 对见过的数据建模，也称自回归模型

### 马尔科夫假设
- 假设当前数据只跟 $\tau$ 个过去数据点有关

### 潜变量模型
- 引入潜变量 $h_t$ 来表示过去信息 $h_t = f(x_1, \cdots x_{t-1})$
  - 这样 $x_t = p(x_t|h_t)$ 