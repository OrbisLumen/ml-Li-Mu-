# 编码器与解码器

## 编码器-解码器架构

*编码器-解码器*（encoder-decoder）架构：
- 第一个组件是一个*编码器*（encoder）：
  - 它接受一个长度可变的序列作为输入，并将其转换为具有固定形状的编码状态
- 第二个组件是*解码器*（decoder）：
  - 它将固定形状的编码状态映射到长度可变的序列

![alt text](encoder-decoder.svg)

