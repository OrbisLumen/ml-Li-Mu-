# Deep-RNN（深度循环神经网络）

使用多个隐藏层来获得更多的非线形性

## 更深

![alt text](deep-rnn.svg)

$$\mathbf{H}_t^{(l)} = \phi_l(\mathbf{H}_t^{(l-1)} \mathbf{W}_{xh}^{(l)} + \mathbf{H}_{t-1}^{(l)} \mathbf{W}_{hh}^{(l)}  + \mathbf{b}_h^{(l)})$$
